services:
  - type: web
    name: comrades-md-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: SESSION_ID
        sync: false
      - key: PREFIX
        value: "."
        sync: false
      - key: OWNER_NUMBER
        sync: false
