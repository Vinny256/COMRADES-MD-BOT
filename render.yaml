services:
  - type: web
    name: comrades-md-bot
    env: docker
    plan: free
    envVars:
      - key: SESSION_ID
        sync: false
      - key: PREFIX
        value: "."
      - key: OWNER_NUMBER
        sync: false
